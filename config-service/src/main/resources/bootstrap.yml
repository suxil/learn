spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/suxi-lu/cloud-config
          search-paths: config-repo

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://admin:admin123@localhost:8761/eureka/
    #enabled: false
